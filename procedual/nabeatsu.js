"use strict;"

// // Express the status of "crazy" with '!'
//
// const oneToForty = [...Array(40).keys()].map(num => ++num);
//
// oneToForty.forEach(num => {
//   if (num % 3 === 0 || String(num).includes("3")) {
//     console.log(`${num}!`);
//   } else {
//     console.log(`${num}`);
//   }
// })
//
// oneToForty.forEach(num => {
//   num % 3 === 0 || String(num).includes("3") ? console.log(`${num}!`) : console.log(`${num}`);
// })
